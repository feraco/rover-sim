# Arduino Code Converter - System Architecture

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    GEARS SIMULATOR                               │
│                                                                  │
│  ┌────────────────────┐                                         │
│  │  BLOCKLY BLOCKS    │                                         │
│  │  (Visual Editor)   │                                         │
│  │                    │                                         │
│  │  [Move Forward]    │                                         │
│  │  [Turn Left]       │                                         │
│  │  [If Distance < 20]│                                         │
│  │  [Follow Line]     │                                         │
│  └──────────┬─────────┘                                         │
│             │                                                    │
│             ▼                                                    │
│  ┌─────────────────────────────────────────────┐               │
│  │     CODE GENERATOR SELECTOR                 │               │
│  │                                              │               │
│  │  [Python Mode] ◄──► [Arduino Mode]         │               │
│  └──────┬──────────────────────┬───────────────┘               │
│         │                      │                                │
│         ▼                      ▼                                │
│  ┌──────────────┐      ┌────────────────────┐                 │
│  │ ev3dev2      │      │ arduino_generator  │                 │
│  │ _generator.js│      │ .js                │                 │
│  │              │      │                    │                 │
│  │ Python Code  │      │ Arduino C++ Code   │                 │
│  └──────┬───────┘      └─────────┬──────────┘                 │
│         │                         │                             │
└─────────┼─────────────────────────┼─────────────────────────────┘
          │                         │
          ▼                         ▼
┌─────────────────┐       ┌──────────────────────┐
│  PYTHON OUTPUT  │       │  ARDUINO OUTPUT      │
│                 │       │                      │
│  #!/usr/bin/... │       │  #include <Car...    │
│  from ev3dev2   │       │  CarControl car(...) │
│  tank_drive.on  │       │  car.moveForward...  │
│  ...            │       │  car.turnLeft...     │
└─────────────────┘       └──────────┬───────────┘
                                     │
                                     ▼
                          ┌──────────────────────┐
                          │  ARDUINO IDE         │
                          │                      │
                          │  [Compile]           │
                          │  [Upload]            │
                          └──────────┬───────────┘
                                     │
                                     ▼
                          ┌──────────────────────┐
                          │  ARDUINO BOARD       │
                          │  + CarControl        │
                          │                      │
                          │  🤖 REAL ROBOT!      │
                          └──────────────────────┘
```

## Data Flow

### Python Mode (Original)
```
Blocks → ev3dev2_generator.js → Python Code → Skulpt → simPython.js → Virtual Robot
```

### Arduino Mode (New!)
```
Blocks → arduino_generator.js → Arduino C++ → Arduino IDE → Physical Robot
```

## Component Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                        FILE STRUCTURE                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  public/                                                          │
│  │                                                                │
│  ├── index.html ─────────────┐                                   │
│  │   • Loads all scripts     │                                   │
│  │   • Initializes UI        │                                   │
│  │                           │                                   │
│  ├── js/                     │                                   │
│  │   ├── blockly.js ◄────────┤ Main initialization              │
│  │   │   • Workspace mgmt    │                                   │
│  │   │   • Loads blocks      │                                   │
│  │   │   • Manages generator │                                   │
│  │   │                       │                                   │
│  │   ├── ev3dev2_generator.js                                    │
│  │   │   • Python generation │                                   │
│  │   │   • EV3 API mapping   │                                   │
│  │   │                       │                                   │
│  │   └── arduino_generator.js ◄── NEW!                          │
│  │       • Arduino C++ gen   │                                   │
│  │       • CarControl mapping│                                   │
│  │                           │                                   │
│  ├── customBlocks.json ◄─────┤ Original blocks                  │
│  │   • Standard blocks       │                                   │
│  │   • EV3 blocks            │                                   │
│  │                           │                                   │
│  └── arduinoBlocks.json ◄────┴─ NEW! Arduino blocks             │
│      • CarControl blocks                                         │
│      • Hardware-specific                                         │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

## Block Processing Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│                    BLOCK TO CODE PIPELINE                        │
└─────────────────────────────────────────────────────────────────┘

1. BLOCK DEFINITION (arduinoBlocks.json)
   ┌────────────────────────────────────┐
   │ {                                  │
   │   "type": "arduino_move_forward",  │
   │   "message0": "move forward...",   │
   │   "args0": [...]                   │
   │ }                                  │
   └─────────────────┬──────────────────┘
                     │
                     ▼
2. BLOCK IN WORKSPACE (Blockly)
   ┌────────────────────────────────────┐
   │  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━┓   │
   │  ┃ move forward              ┃   │
   │  ┃ speed [200] for [1000] ms ┃   │
   │  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━┛   │
   └─────────────────┬──────────────────┘
                     │
                     ▼
3. GENERATOR FUNCTION (arduino_generator.js)
   ┌────────────────────────────────────┐
   │ 'arduino_move_forward': function { │
   │   var speed = getCode('speed');    │
   │   var dur = getCode('duration');   │
   │   return 'car.moveForward(' +      │
   │          speed + ', ' + dur + ')'; │
   │ }                                  │
   └─────────────────┬──────────────────┘
                     │
                     ▼
4. GENERATED CODE
   ┌────────────────────────────────────┐
   │ car.moveForward(200, 1000);        │
   └────────────────────────────────────┘
```

## Integration Points

```
┌────────────────────────────────────────────────────────────────┐
│                     INTEGRATION DIAGRAM                         │
└────────────────────────────────────────────────────────────────┘

USER INTERFACE
    │
    ├─ Blocks Tab ──────► blocklyPanel.js
    │                          │
    │                          ├─ Blockly Workspace
    │                          └─ Block Toolbox
    │
    ├─ Python/Arduino Tab ──► pythonPanel.js
    │                          │
    │                          └─ ACE Editor (shows generated code)
    │
    └─ Sim Tab ─────────────► simPanel.js (virtual robot)

GENERATOR SELECTION
    │
    ├─ Python Mode
    │   └─► ev3dev2_generator.genCode()
    │       └─► Python code string
    │
    └─ Arduino Mode
        └─► arduino_generator.genCode()
            └─► Arduino C++ code string

CODE EXECUTION
    │
    ├─ Python: skulpt.js → simPython.js → Virtual Robot
    │
    └─ Arduino: Copy to Arduino IDE → Upload → Physical Robot
```

## Block Categories

```
┌─────────────────────────────────────────────────────────────┐
│                    BLOCK CATEGORIES                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  MOVEMENT (Arduino)                                         │
│  ┌────────────────────────────────────────────────┐        │
│  │ • arduino_move_forward      ───► car.moveForward()     │
│  │ • arduino_move_backward     ───► car.moveBackward()    │
│  │ • arduino_turn_left         ───► car.turnLeft()        │
│  │ • arduino_turn_right        ───► car.turnRight()       │
│  │ • arduino_stop_motors       ───► car.stopMotors()      │
│  │ • arduino_follow_line       ───► car.followLine()      │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  SENSORS (Arduino)                                          │
│  ┌────────────────────────────────────────────────┐        │
│  │ • arduino_get_distance      ───► car.getDistanceToObstacle() │
│  │ • arduino_line_sensor_left  ───► car.getLineSensorLeft()     │
│  │ • arduino_line_sensor_middle───► car.getLineSensorMiddle()   │
│  │ • arduino_line_sensor_right ───► car.getLineSensorRight()    │
│  │ • arduino_battery_level     ───► car.getBatteryLevel()       │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  ACTUATORS (Arduino)                                        │
│  ┌────────────────────────────────────────────────┐        │
│  │ • arduino_look_left         ───► car.lookLeft()        │
│  │ • arduino_look_right        ───► car.lookRight()       │
│  │ • arduino_look_center       ───► car.centerServo()     │
│  │ • arduino_open_claw         ───► car.openClaw()        │
│  │ • arduino_close_claw        ───► car.closeClaw()       │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  SOUND & LIGHT (Arduino)                                    │
│  ┌────────────────────────────────────────────────┐        │
│  │ • arduino_beep              ───► car.beep()            │
│  │ • arduino_play_star_wars    ───► car.playStarWars()   │
│  │ • arduino_light_rgb         ───► car.lightRGBForDuration() │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  STANDARD BLOCKS (Both)                                     │
│  ┌────────────────────────────────────────────────┐        │
│  │ • Controls (if, loops, repeat)                         │
│  │ • Logic (and, or, not, compare)                        │
│  │ • Math (arithmetic, numbers)                           │
│  │ • Variables (get, set)                                 │
│  │ • Text (strings, print)                                │
│  └────────────────────────────────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Hardware Mapping

```
┌───────────────────────────────────────────────────────────────┐
│              ARDUINO + CarControl HARDWARE MAP                 │
├───────────────────────────────────────────────────────────────┤
│                                                                │
│   ARDUINO BOARD                                               │
│   ┌─────────────────────────────────────────────────┐        │
│   │                                                  │        │
│   │  Digital Pins:                                  │        │
│   │  ├─ 3  → STBY (Motor Standby)                  │        │
│   │  ├─ 4  → RGB LED Data                           │        │
│   │  ├─ 5  → PWMA (Motor A PWM)                     │        │
│   │  ├─ 6  → PWMB (Motor B PWM)                     │        │
│   │  ├─ 7  → AIN (Motor A Direction)                │        │
│   │  ├─ 8  → BIN (Motor B Direction)                │        │
│   │  ├─ 9  → Claw Servo                             │        │
│   │  ├─ 10 → Sensor Servo                           │        │
│   │  ├─ 11 → Buzzer                                 │        │
│   │  ├─ 12 → Ultrasonic Echo                        │        │
│   │  └─ 13 → Ultrasonic Trigger                     │        │
│   │                                                  │        │
│   │  Analog Pins:                                   │        │
│   │  ├─ A0 → Right Line Sensor / IR Sensor         │        │
│   │  ├─ A1 → Middle Line Sensor                     │        │
│   │  ├─ A2 → Left Line Sensor                       │        │
│   │  └─ A3 → Battery Monitor                        │        │
│   │                                                  │        │
│   └─────────────────────────────────────────────────┘        │
│                                                                │
│   CONNECTED HARDWARE                                          │
│   ┌────────────────────────────────────────────────┐         │
│   │  Motors: 2x DC Motors                          │         │
│   │  Sensors: Ultrasonic, 3x Line, IR              │         │
│   │  Servos: 2x (Sensor Pan, Claw)                │         │
│   │  Output: Buzzer, RGB LED                        │         │
│   └────────────────────────────────────────────────┘         │
│                                                                │
└───────────────────────────────────────────────────────────────┘
```

## Usage Workflow

```
┌──────────────────────────────────────────────────────────┐
│                  TYPICAL USAGE FLOW                       │
└──────────────────────────────────────────────────────────┘

STEP 1: Design in Simulator
    │
    ├─► Open Gears Simulator
    ├─► Switch to Arduino Mode
    └─► Drag Arduino blocks to workspace

STEP 2: Generate Code
    │
    ├─► Blocks automatically convert to Arduino C++
    ├─► View generated code in Code tab
    └─► Test logic (optional simulation)

STEP 3: Deploy to Hardware
    │
    ├─► Copy generated Arduino code
    ├─► Paste into Arduino IDE
    ├─► Verify CarControl library installed
    └─► Upload to Arduino board

STEP 4: Test Physical Robot
    │
    ├─► Robot executes uploaded program
    ├─► Observe sensors and motors
    └─► Debug and iterate

STEP 5: Refine (if needed)
    │
    ├─► Return to simulator
    ├─► Modify blocks
    ├─► Regenerate code
    └─► Re-upload
```

---

**This visual guide shows how all components work together to transform visual blocks into executable Arduino code for your CarControl robot!** 🤖
